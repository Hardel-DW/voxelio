{
    "license": "MIT",
    "type": "module",
    "scripts": {
        "build": "pnpm run build --recursive",
        "build:watch": "pnpm run build:watch --recursive",
        "biome:format": "biome format --write ./packages",
        "biome:check": "biome check ./packages",
        "biome:unsafefix": "biome check --write --unsafe  ./packages",
        "test": "vitest run",
        "check": "tsc --noEmit"
    },
    "devDependencies": {
        "@biomejs/biome": "^2.0.6",
        "tsdown": "^0.12.9",
        "vitest": "^3.2.4"
    },
    "dependencies": {
        "typescript": "^5.8.3"
    }
}